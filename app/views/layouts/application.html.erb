<!DOCTYPE html>
<html>
  <head>
    <title>RubyGarments</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <%= javascript_importmap_tags %>
    <link rel="preload" href="/assets/application.css" as="style">
    <link rel="stylesheet" href="/assets/application.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Spectral:ital,wght@0,200;0,300;0,400;0,500;0,600;0,700;0,800;1,200;1,300;1,400;1,500;1,600;1,700;1,800&display=swap" rel="stylesheet">
  </head>

    <body class="<%= "#{controller_name}-#{action_name} #{controller_name == 'garments' && action_name == 'index' ? 'index-page' : ''} #{controller_name == 'garments' && %w[new edit].include?(action_name) ? 'form-page' : ''}" %>">
      <% unless controller_name == 'sessions' || controller_name == 'registrations' %>
        <%= render 'shared/navbar' %>
      <% end %>

      <%= render "shared/flashes" %>
      <%= yield %>
    </body>
</html>
